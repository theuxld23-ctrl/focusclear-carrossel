python-dotenv
groq
anthropic
requests
playwright
pillow
python-telegram-bot
imageio-ffmpeg   # binário ffmpeg estático (montagem do reel) quando não há ffmpeg no PATH

# Fase 1 — backend como serviço
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
sqlalchemy>=2.0.0
apscheduler>=3.10.0
aiofiles>=23.0.0
python-multipart>=0.0.9

# Testes (suíte offline roda tanto como módulo quanto via pytest)
pytest>=8.0.0
